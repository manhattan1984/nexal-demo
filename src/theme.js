import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    // background: {
    //   default: "gray",
    // },
    // mode: "dark",
    primary: {
      main: "#fcba03",
    },
    secondary: {
      main: "#19857b",
    },
  },
});
export default theme;
